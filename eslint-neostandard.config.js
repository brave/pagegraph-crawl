// This eslint config is used for checking the code in this project written in
// JS (e.g., test/*.js). The other eslint config file in this project,
// eslint.config.js, is used for linting the typescript in this project
// (e.g., src/*).

import neostandard from 'neostandard'

export default neostandard({})
